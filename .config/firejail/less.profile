# The default less profile blacklists a lot of configuration files that can be
# useful to browse with less. I also may want to use vim from less, so I'll just
# use vim's profile.
quiet
include ${HOME}/.config/firejail/vim.profile
