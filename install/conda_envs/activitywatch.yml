name: activitywatch
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
    # activitywatch uses poetry to lock deps, so we don't use pip-tools to avoid
    # conflicts between them.
    - poetry
